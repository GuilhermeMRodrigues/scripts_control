using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class position : MonoBehaviour
{
    // Start is called before the first frame update
    [SerializeField] private float monitorX, monitorY;
    void Start()
    {
        //GameObject.FindWithTag("GameManager").GetComponent<Alou>().setAvatarPosition(monitorX, monitorY);
    
    }
}
